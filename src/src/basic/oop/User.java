package basic.oop;

public class User {
    static void bubble(int[] arr) {
        //int[] ints = {16, 10, 8, 25, 6, 1};

        //for (int i = 0; i < ints.length; i++){
        //    int first = ints[i];
        //    int second = ints[i + 1];
        //    if (ints[i] > ints[i+1]){
        //        int x = ints[i];
        //        ints[i] = ints[i+1];
        //        ints[i+1] = x;
        //    }
        //}
    }
}
